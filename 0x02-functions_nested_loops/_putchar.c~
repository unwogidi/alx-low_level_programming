#include <unistd.h>
#include "main.h"

/**
 * _putchar - writes a c character to stdout
 * c : charcter to print
 * Return: Always 1 (Success)
 * On error -1 is returned
 */
int _putchar(char c)
{
return (write(1, &c, 1));
}
