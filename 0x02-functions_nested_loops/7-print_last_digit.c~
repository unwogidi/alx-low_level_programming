#include "main.h"

/**
 * print_last_digit - print the last digit of a number
 * @num: a number
 * Return: a digit success
 */
int print_last_digit(int num)
{
int last_digit;

last_digit = num % 10;
return (last_digit);
}
